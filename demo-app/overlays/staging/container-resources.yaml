apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
spec:
  template:
    spec:
      containers:
      - name: demo-app
        resources:
          limits:
            cpu: "200M"
            memory: "200Mi"
          requests:
            cpu: "200M"
            memory: "200Mi"
